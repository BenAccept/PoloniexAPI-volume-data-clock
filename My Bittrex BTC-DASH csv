import csv
import operator

sample = open('BTC-DASH.csv','r')

##sort = sorted(csv1, key=operator.itemgetter(0))

csv1 = csv.reader(sample,delimiter=',')

for eachline in sort:
    print eachline
    
class subsetDelimitersuniversebreaker(self,csv1):
    number_of_delimiters = 9 #because we have nine subset lists. I could make the class a panda list subset
    self.closed_date
    self.opened_date
    self.market
    self.type
    self.bid_ask
    self.unites_filled_DASH
    self.unites_total_DASH
    self.actual_rate
    self.cost_proceeds

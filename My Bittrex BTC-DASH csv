import csv
import time
#Definition of DASH-CSV
sample = open('BTC-DASH.csv','r')
sort = sorted(csv1, key=operator.itemgetter(0))
csv1 = csv.reader(sample,delimiter=',')
for eachline in sort:
    print eachline

class metasubset(x, dict):
    x = {}
    csvnums = [ cost_proceeds, 
                actual_rate, 
                unites_total_DASH, 
                unites_filled_DASH, 
                bid_ask, 
                type, 
                market, 
                opened_date, 
                closed_date ]
    
    x = csvnums * [0] + [1] + [4] + [5] + [6] + [7] + [8] # = 0
    x = metasubset({}.format()) #possibly append at the end here or on too x
    
#Build printout, creates array
csvnums[0] = str("CSV total: ").append(x)
csvnums[1] = str("CSV maker taker fee: ").append(x)
csvnums[2] = str("CSV total unites: ").append(x)
csvnums[3] = str("CSV filled: ").append(x)
csvnums[4] = str("CSV buy or sell price: ").append(x)
csvnums[5] = str("CSV buy or sell: ").append(x)
csvnums[6] = str("CSV what crypto: ").append(x)
csvnums[7] = str("CSV when did I open a market: ").append(x)
csvnums[8] = str("CSV when and how long I held the market: ").append(x)

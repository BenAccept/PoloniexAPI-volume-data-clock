import csv
import operator

sample = open('BTC-DASH.csv','r')

##sort = sorted(csv1, key=operator.itemgetter(0))

csv1 = csv.reader(sample,delimiter=',')

for eachline in sort:
    print eachline
    
class subsetDelimitersuniversebreaker(self, operator, delimiter):
    number_of_delimiters = 9 #because we have nine subsets. I could make the class a concatenated string panda list subset
    self.operator.closed_date
    self.operator.opened_date
    self.operator.market
    self.operator.type
    self.operator.bid_ask
    self.operator.unites_filled_DASH
    self.operator.unites_total_DASH
    self.operator.actual_rate
    self.operator.cost_proceeds

#make an area and insert the correct delimiter area
#example print| be print(x[1], x[9]) str()
#I should replace a list before I maybe print

class metasubset(self, delimiter):
    operator = [ 
    self.cost_proceeds + "CSV total: ",
    self.actual_rate + "CSV maker taker fee: ",
    self.unites_total_DASH +"CSV total unites of DASH: ",
    self.unites_filled_DASH + "CSV filled DASH: ",
    self.bid_ask + "CSV DASH buy or sell price: ",
    self.type + "CSV type buy or sell: ",
    self.market + "CSV what crypto scale: ",
    self.opened_date + "CSV when did I open a market: ",
    self.closed_date + "CSV when and how long I held the market: "
    ]
